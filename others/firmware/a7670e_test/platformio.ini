; Standalone SIMCom A7670E test - no main project deps (no PZEM, TinyGSM, etc.)
; Run from this directory: pio run && pio run -t upload && pio device monitor -b 115200

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
